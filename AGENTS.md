# AGENTS.md — CDLC operating rules

このファイルは、このリポジトリで作業するすべての AI エージェントに適用する。目的は、プロンプトの工夫だけに依存せず、AI に与えるコンテキストを検証可能な開発資産として管理することである。

本書では Context Development Lifecycle の略称を **CDLC** とする。

## 1. 基本原則

- コンテキストをコードと同じように、収集・選別・テスト・配布・観察・改善する。
- コンテキストには、ユーザー要求、`AGENTS.md`、仕様、ソースコード、テスト、設計判断、実行結果、ツール権限、会話履歴、外部資料を含む。
- エージェントの記憶や推測を事実として扱わない。リポジトリ、実行結果、ユーザー提供情報、一次資料を根拠にする。
- 「大量の情報」ではなく「現在の判断に必要十分な情報」を使う。無関係な情報で重要な制約を埋没させない。
- コンテキストの変更は挙動を変える変更である。レビュー可能、追跡可能、取り消し可能にする。
- 自動生成物を自動的に正解へ昇格させない。共有ルールにする前に、根拠と再現可能な評価を確認する。

## 2. 指示と根拠の優先順位

矛盾がある場合は、次の順に従う。

1. システム、開発者、ユーザーから現在のセッションに与えられた指示
2. 作業対象に最も近い階層の `AGENTS.md`
3. 実行可能なテスト、型、スキーマ、設定、公開インターフェース
4. リポジトリの現行コードと現在有効な設計資料
5. README、コメント、過去の記録
6. 一般知識と推測

優先順位だけで安全に解決できない仕様矛盾は、勝手に選ばずユーザーへ明示する。コードと文書の不一致を見つけた場合も、どちらかを無条件に正しいと決めない。

## 3. 必須の CDLC サイクル

すべての変更タスクで、規模に応じて以下を実施する。小さな変更では簡潔に行ってよいが、省略した工程がある場合は理由を説明できるようにする。

### 3.1 Generate — 原材料を集める

作業開始時に、次を確認する。

- ユーザーが求める成果、対象範囲、対象外、制約
- 適用されるすべての `AGENTS.md`
- 関連する実装、テスト、設定、型、ドキュメント
- 作業ツリーの状態と、ユーザーがすでに加えた変更
- バグ修正なら、可能な範囲で現象と再現条件
- 外部仕様に依存するなら、その時点で有効な一次資料

暗黙の要求を補う必要がある場合は、事実・推論・仮定を区別する。結果を大きく変える不明点だけを質問し、それ以外は合理的な仮定を明示して進める。

### 3.2 Compile — タスク用コンテキストを組み立てる

変更前に、少なくとも次を自分の中で固定する。

- 目的を一文で表したもの
- 変更してよい範囲と変更しない範囲
- 観察可能な受け入れ条件
- 参照する根拠と、各根拠が必要な理由
- 実行する検証と、成功の判定方法

探索は狭い検索から始め、必要に応じて広げる。巨大なファイル、ログ、検索結果を丸ごと読み込まず、該当箇所を選ぶ。同じ目的でも、実装時には関連コードと仕様を、レビュー時には受け入れ条件・差分・検証結果を優先する。

### 3.3 Test — コンテキストと成果を評価する

- 実装前に、受け入れ条件をテスト可能な形にする。
- バグ修正では、可能なら修正前に失敗を再現し、回帰テストを追加する。
- 文言や指示の品質ではなく、生成された挙動・コード・画面が条件を満たすか評価する。
- まず変更箇所に近い検証を実行し、その後に影響範囲に応じた広い検証を行う。
- 非決定的な AI 出力を評価する場合は、一致する文章ではなく、必須条件・禁止条件・許容範囲で判定する。重要なケースは複数回または複数例で確認する。
- テストを実行できない場合は、未実行の項目、理由、残るリスクを報告する。「おそらく動く」を合格扱いしない。

### 3.4 Distribute — 再利用可能なコンテキストを配布する

タスク固有の気づきを共有ルールへ昇格させるのは、次を満たす場合に限る。

- 複数の将来タスクで再利用できる。
- 実装、テスト、一次資料、または反復した失敗という根拠がある。
- 適用範囲、所有対象、失効条件を説明できる。
- 既存ルールと重複または矛盾しない。

共有コンテキストは、適切な階層の `AGENTS.md`、テスト、型、設定、または正式な設計資料へ置く。一時的な作業メモや会話全文を恒久ルールとしてコピーしない。秘密情報、トークン、個人情報、不要な本番データを含めない。

### 3.5 Deliver — 必要なコンテキストが届くようにする

- ルールは、その影響を受けるファイルから発見できる場所に置く。
- 詳細資料を追加した場合は、入口となる文書から参照できるようにする。
- ファイルが存在するだけでエージェントに読まれたと仮定しない。パス、適用範囲、読み込み条件を確認する。
- 重要な制約は、無関係な長文の中に一度だけ埋め込まない。
- 同じ情報を複数箇所へ複製するより、正本を一つ決めて参照する。

### 3.6 Observe — 実際の結果を観察する

変更後は、次を証拠として確認する。

- 差分が要求された範囲に収まっているか
- テスト、型検査、lint、build の終了状態と重要な出力
- UI 変更なら、対象画面、主要操作、レスポンシブ表示、アクセシビリティ
- ユーザーの修正、レビュー指摘、再発した失敗

失敗をすべてプロンプトの問題にしない。少なくとも「コード」「仕様またはコンテキスト」「ツール」「環境」「一時的障害」の可能性を切り分ける。実行していない検証を実行済みと報告しない。

### 3.7 Adapt — 根拠に基づいて改善する

- 観察された失敗が再発し得る場合は、可能なら回帰テストを先に追加する。
- 不足した仕様は明確化し、古い情報は更新または削除し、矛盾は正本へ統合する。
- 一度きりの偶発事象を、検証なしに全体ルールへ一般化しない。
- 新しい共有指示を加えたら、その指示が代表的なタスクを改善し、既存挙動を損なわないことを確認する。
- 自分自身で追加した指示を、自分の判断だけで「検証済み」としない。独立レビューが利用可能かつ許可されていれば利用し、そうでなければ固定した受け入れ条件と客観的な検証結果でセルフレビューする。

## 4. 標準作業フロー

各タスクは原則として次の順序で進める。

1. 要求を読み、目的・範囲・受け入れ条件を整理する。
2. 適用指示、関連コード、テスト、作業ツリーを調査する。
3. 最小限の変更方針と検証方針を決める。
4. ユーザーの既存変更を保持し、必要な箇所だけ編集する。
5. 変更箇所に近いテストを実行する。
6. 影響に応じて型検査、全テスト、lint、build、UI テストを実行する。
7. 最終差分と実行結果を受け入れ条件に照らしてレビューする。
8. 再利用可能で検証済みの学びだけを、テストまたは正本の文書へ反映する。
9. 成果、検証結果、未解決事項を簡潔に報告する。

## 5. このリポジトリでの検証コマンド

Node.js は `package.json` の `engines` を満たす版を使用する。Windows PowerShell で実行ポリシーの影響を避ける必要がある場合は `npm` の代わりに `npm.cmd` を使用できる。

```powershell
npm.cmd run typecheck
npm.cmd test
npm.cmd run lint
npm.cmd run build
npm.cmd run test:ui
```

用途別テストも利用できる。

```powershell
npm.cmd run test:sql
npm.cmd run test:labs
npm.cmd run test:feedback
```

すべての変更で全コマンドを機械的に実行する必要はない。最低限、変更に直接対応する検証を行い、共有コード、設定、依存関係、公開前の変更では広い検証を行う。ドキュメントだけの変更では、内容、リンク、コマンド、差分のレビューを検証とすることができる。

## 6. コンテキストの品質基準

良いコンテキストは次を満たす。

- **正確**: 現在のコードと有効な仕様に一致する。
- **関連的**: 対象タスクの判断に必要である。
- **十分**: 成功条件、制約、例外が欠けていない。
- **簡潔**: 同じ情報を重複させず、重要事項を埋没させない。
- **追跡可能**: 根拠または正本を特定できる。
- **評価可能**: 守られたかをテストまたはレビューで判断できる。
- **保守可能**: 適用範囲と更新・削除の条件が分かる。
- **安全**: 権限、秘密情報、外部入力を適切に扱う。

## 7. 禁止するアンチパターン

- リポジトリを確認せず、記憶だけで実装する。
- 問題を再現または調査せず、推測した原因だけを修正する。
- コンテキスト不足を、長い万能プロンプトの追記だけで解決する。
- 関連性を評価せず、リポジトリ全体や大量ログをコンテキストへ投入する。
- テストに合格させるために、正しい仕様ではなくテストだけを弱める。
- 一回の成功例を普遍的なルールとして共有する。
- 古い指示を残したまま、新しい矛盾する指示を追加する。
- ユーザーの既存変更を上書き、削除、または無関係に整形する。
- 根拠のない完了宣言や、実行していない検証の報告をする。
- コンテキスト改善を名目に、依頼範囲外の大規模な整理を行う。

## 8. 完了条件と最終報告

タスクは次のすべてを満たしたときに完了とする。

- ユーザーの要求と受け入れ条件を満たしている。
- 差分が意図した範囲に限定されている。
- 必要な検証が成功している、または実行不能な理由とリスクが明示されている。
- 関連する文書、テスト、設定の不整合を残していない。
- 秘密情報、不要な生成物、デバッグ用変更を混入させていない。

最終報告には、原則として次を含める。

- 何を変更し、どの成果が得られたか
- 実行した検証と結果
- 未実行の検証、既知の制約、残るリスク（存在する場合のみ）
- 更新した共有コンテキストと、その根拠（存在する場合のみ）

報告は簡潔にするが、検証の証拠と不確実性は省略しない。
